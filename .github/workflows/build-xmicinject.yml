name: Build XMicInject APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout build repo
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Clone upstream XMicInject
        run: git clone --depth 1 https://github.com/n00b-labs/XMicInject.git upstream

      - name: Make Gradle wrapper executable
        run: chmod +x upstream/gradlew

      - name: Build debug APK
        working-directory: upstream
        run: ./gradlew --no-daemon assembleDebug

      - name: Rename APK
        run: cp upstream/app/build/outputs/apk/debug/app-debug.apk XMicInject.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: XMicInject-APK
          path: XMicInject.apk
          if-no-files-found: error
